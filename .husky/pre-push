#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

echo "🚀 Running pre-push checks..."

cd simple-product-shop || exit 1

# Unit tests
echo "🧪 Running tests..."
pnpm test -- --run || exit 1

# Build
echo "📦 Building..."
pnpm build || exit 1

echo "✅ Pre-push checks passed!"
